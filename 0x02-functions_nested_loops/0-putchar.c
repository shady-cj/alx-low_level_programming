#include <stdio.h>

/**
 * main - prints the number of arguments passed to the program(Entry point)
 * Return: Always 0 (Success)
 * 
 */

int main(void)
{
    printf("_putchar\n");
    return (0); 
}